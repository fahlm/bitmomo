<?php
/**
 * Minimal WordPress stub for standalone executable tests — defines just
 * enough (ABSPATH) for the plugin's pure-PHP classes to load outside a
 * real WordPress environment. Same convention as bitmomo-pro and
 * bitmomo-regime's test stubs.
 */

if ( ! defined( 'ABSPATH' ) ) {
	define( 'ABSPATH', __DIR__ . '/' );
}
